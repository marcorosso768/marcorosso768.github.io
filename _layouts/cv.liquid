---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <style>
        /* Flex container to center the iframe */
        .iframe-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
        }

        /* Responsive iframe */
        iframe {
            width: 100%; /* Full width for small screens */
            max-width: 1200px; /* Maximum width for large screens */
            border: none;
            height: 80vh; /* Use viewport height to scale the iframe */
            overflow: auto; /* Enable scrolling within the iframe */
        }
        
        /* Hide iframe and show the message on mobile devices */
        @media (hover: none) and (pointer: coarse) {
            .iframe-container {
                display: none; /* Hide iframe container on mobile devices */
            }
            .mobile-message {
                display: block; /* Show the mobile message */
                background-color: yellow; /* For debugging, apply a visible background color */
                color: red; /* Set text color for visibility */
            }
        }

        /* Default setting for the mobile message (hidden) */
        .mobile-message {
            display: none;
        }
        
    </style>
</head>
    <body>
        <!-- Title -->
        <div class="post">
            <header class="post-header">
            <h1 class="post-title" style="color: var(--global-theme-color);">{{ page.title }}
            <a
                href="{{ "/assets/pdf/[Marco Rosso] Curriculum Vitae.pdf" | relative_url }}"
                download="[Marco Rosso] Curriculum Vitae.pdf"
                target="_blank"
                rel="noopener noreferrer"
                class="float-right"
                style="color: var(--global-text-color);"
                ><i class="fa-solid fa-file-pdf"></i
              ></a>
            </h1>
            <p class="post-description">{{ page.description }}</p>
            </header>
        </div>
    
        <!-- Embed the PDF using iframe for better mobile support -->
        <div class="iframe-container">
            <iframe src="{{ site.baseurl }}/assets/pdf/[Marco Rosso] Curriculum Vitae.pdf" title="curriculum vitae"></iframe>
        </div>
    
        <!-- Fallback message for mobile devices -->
        <div class="mobile-message">
            <p>I am sorry, but the CV cannot be displayed on your device. Please click the PDF icon to view or download the CV.</p>
        </div>
    </body>
</html>
