---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* Flex container to center the download button */
        .button-container {
            display: flex;
            justify-content: center; /* Centers the button horizontally */
            align-items: center; /* Vertically aligns the button */
            margin: 20px 0; /* Adds space around the button */
        }

        /* Style for the download button */
        .download-button {
            padding: 10px 20px;
            border-radius: 5px;
            text-transform: lowercase;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
            display: inline-block;
        }

        /* Embed the PDF via iframe */
        .iframe-container {
            position: relative;
            width: 100%;
            max-width: 1200px;
            height: 0;
            padding-bottom: 141.67%; /* 8.5x11 ratio to keep the aspect correct for PDF */
            overflow: hidden; /* Ensures the iframe doesn't exceed its container */
        }

        iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Scroll on smaller screens */
        .iframe-container iframe {
            overflow: auto; /* Allows scrolling for PDF pages */
        }

        /* Responsive behavior for tablets (iPad) */
        @media (max-width: 1024px) {
            .iframe-container {
                padding-bottom: 125%; /* Adjust height ratio for iPad */
            }
        }

        /* Responsive behavior for mobile devices */
        @media (max-width: 768px) {
            .iframe-container {
                padding-bottom: 150%; /* Adjust height ratio for smaller screens */
            }

            .download-button {
                width: 80%;
                max-width: 300px;
            }
        }

        /* Extra small mobile devices */
        @media (max-width: 480px) {
            .iframe-container {
                padding-bottom: 170%; /* Adjust height ratio for very small screens */
            }

            .download-button {
                width: 90%;
            }
        }
    </style>
</head>
<body>

    <!-- Title -->
    <div class="post">
        <header class="post-header">
        <h1 class="post-title">{{ page.title }}</h1>
        <p class="post-description">{{ page.description }}</p>
        </header>
    </div>

    <!-- Download button centered on the page -->
    <div class="button-container">
        <a href="{{ "/assets/pdf/cv-light.pdf" | relative_url }}" download="[Marco Rosso] Curriculum Vitae.pdf" class="download-button">
            download curriculum vitae
        </a>
    </div>

    <!-- Embed the PDF using iframe for better mobile support -->
    <div class="iframe-container">
        <iframe src="{{ site.baseurl }}/assets/pdf/cv-light.pdf" title="Curriculum Vitae"></iframe>
    </div>

</body>
</html>
