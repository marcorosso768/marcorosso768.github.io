---
layout: default
---

<div style="position: relative; width: 100%; height: 0; padding-bottom: 75%;">
    <!-- Light mode PDF -->
    <iframe id="light-mode" src="{{ '/assets/pdf/cv-light.pdf' | relative_url }}" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;" frameborder="0"></iframe>
    <!-- Dark mode PDF -->
    <iframe id="dark-mode" src="{{ '/assets/pdf/cv-dark.pdf' | relative_url }}" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; display: none;" frameborder="0"></iframe>
</div>

<script>
  // Check if user prefers dark mode
  const darkModeMediaQuery = window.matchMedia('(prefers-color-scheme: dark)');

  function handleColorSchemeChange(e) {
      if (e.matches) {
          // Show dark mode PDF
          document.getElementById('light-mode').style.display = 'none';
          document.getElementById('dark-mode').style.display = 'block';
      } else {
          // Show light mode PDF
          document.getElementById('light-mode').style.display = 'block';
          document.getElementById('dark-mode').style.display = 'none';
      }
  }

  // Initial check
  handleColorSchemeChange(darkModeMediaQuery);

  // Listen for color scheme changes
  darkModeMediaQuery.addListener(handleColorSchemeChange);
</script>
