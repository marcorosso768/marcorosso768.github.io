---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CV</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            overflow: hidden; /* Prevent scrollbars */
        }

        /* Style for the download button */
        .download-button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            text-transform: lowercase;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
        }

        /* Embed the PDF */
        embed {
            width: 100%;
            max-width: 800px;
            height: calc(100vh - 80px); /* 80px accounts for the button and margin */
        }

        /* Responsive behavior for mobile devices */
        @media (max-width: 768px) {
            embed {
                height: calc(100vh - 60px); /* Adjusted for mobile */
            }

            .download-button {
                width: 90%;
                max-width: 300px;
            }
        }
    </style>
</head>
<body>

    <!-- Download button centered on the page -->
    <a href="{{ "/assets/pdf/cv-light.pdf" | relative_url }}" download="[Marco Rosso] Curriculum Vitae.pdf" class="download-button">
        download curriculum vitae
    </a>

    <!-- Embed the PDF -->
    <embed src="{{ site.baseurl }}/assets/pdf/cv-light.pdf" type="application/pdf">

</body>
</html>
