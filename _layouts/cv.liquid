---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        /* Flex container to center the download button */
        .button-container {
            display: flex;
            justify-content: center; /* Centers the button horizontally */
            align-items: center; /* Vertically aligns the button */
            margin: 20px 0; /* Adds space around the button */
        }

        /* Style for the download button */
        .download-button {
            padding: 10px 20px;
            border-radius: 5px;
            text-transform: lowercase;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
            display: inline-block;
        }

        /* Embed the PDF */
        embed {
            width: 100%;
            max-width: 1200px;
            height: 1500px; /* Approximate for 3 pages on large screens */
        }

        /* Responsive behavior for tablets (iPad) */
        @media (max-width: 1024px) {
            embed {
                height: 1200px; /* Adjust height for tablets like iPads */
            }
        }

        /* Responsive behavior for mobile devices */
        @media (max-width: 768px) {
            embed {
                height: 900px; /* Adjust height for smaller screens */
            }

            .download-button {
                width: 80%; /* Ensure button width scales well on small screens */
                max-width: 300px;
            }
        }

        /* Extra small mobile devices */
        @media (max-width: 480px) {
            embed {
                height: 800px; /* Further reduce height for very small screens */
            }

            .download-button {
                width: 90%; /* Ensure button takes up more space on tiny screens */
            }
        }
    </style>
</head>
<body>

    <!-- Title -->
    <div class="post">
        <header class="post-header">
        <h1 class="post-title">{{ page.title }}</h1>
        <p class="post-description">{{ page.description }}</p>
        </header>
    </div>

    <!-- Download button centered on the page -->
    <div class="button-container">
        <a href="{{ "/assets/pdf/cv-light.pdf" | relative_url }}" download="[Marco Rosso] Curriculum Vitae.pdf" class="download-button">
            download curriculum vitae
        </a>
    </div>

    <!-- Embed the PDF -->
    <embed src="{{ site.baseurl }}/assets/pdf/cv-light.pdf" type="application/pdf" title="Curriculum Vitae">

</body>
</html>
