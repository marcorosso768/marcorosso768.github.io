---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <style>
        /* Flex container with scrollable overflow */
        .iframe-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            margin: 20px 0;
            overflow: auto; /* Add scrolling capability to the iframe container */
        }

        /* Responsive iframe */
        iframe {
            width: 100%;
            max-width: 1200px;
            border: none;
            height: 80vh;
            overflow: scroll; /* Allow scroll inside the iframe */
            -webkit-overflow-scrolling: touch; /* Enable smooth scrolling on iOS */
        }

        /* Download button */
        .button-container {
            display: flex;
            justify-content: center; /* Centers the button horizontally */
            align-items: center;
            margin: 20px 0;
        }

        .download-button {
            padding: 10px 20px;
            border-radius: 5px;
            text-transform: lowercase;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
            text-decoration: none;
            margin-bottom: 20px;
            display: inline-block;
        }

        /* Responsive for tablet-sized screens */
        @media (max-width: 1024px) {
            iframe {
                max-width: 90vw; /* Adjust width to 90% of the viewport width */
                height: 70vh;    /* 70% of the viewport height */
            }
        }
        
        /* Responsive for smaller tablets and large mobile phones */
        @media (max-width: 768px) {
            iframe {
                max-width: 95vw; /* Slightly wider for mobile */
                height: 65vh;    /* Increase the height for tablets */
            }
        }
        
        /* Responsive for small mobile screens */
        @media (max-width: 480px) {
            iframe {
                max-width: 100vw; /* Use the full viewport width */
                height: 60vh;     /* Adjust the height to fit smaller screens */
            }
        }
    </style>

</head>
<body>
    <!-- Title -->
    <div class="post">
        <header class="post-header">
        <h1 class="post-title" style="color: var(--global-theme-color);">{{ page.title }}
        <a
            href="{{ "/assets/pdf/[Marco Rosso] Curriculum Vitae.pdf" | relative_url }}"
            download="[Marco Rosso] Curriculum Vitae.pdf"
            target="_blank"
            rel="noopener noreferrer"
            class="float-right"
            style="color: var(--global-text-color);"
            ><i class="fa-solid fa-file-pdf"></i
          ></a>
        </h1>
        <p class="post-description">{{ page.description }}</p>
        </header>
    </div>

    <!-- Embed the PDF using iframe for better mobile support -->
    <div class="iframe-container">
        <iframe src="{{ site.baseurl }}/assets/pdf/[Marco Rosso] Curriculum Vitae.pdf" title="curriculum vitae"></iframe>
    </div>
</body>
</html>
