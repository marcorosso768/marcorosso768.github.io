---
layout: default
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CV</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            margin: 0;
            padding: 0;
        }

        .download-button {
            margin-top: 60px;
            padding: 10px 20px;
            border: none;
            text-align: center;
            text-transform: lowercase;
            cursor: pointer;
            font-size: 16px;
            border-radius: 5px;
        }

        .pdf-container {
            margin-top: 20px;
            width: 100%;
            max-width: 800px;
            height: 100vh;
        }

        iframe, embed {
            width: 100%;
            height: 100%;
        }
    </style>
</head>
<body>
    <!-- Download button -->
    <div id="download-btn-container" style="text-align: center; margin-bottom: 20px;">
        <a id="download-btn" href="{{ "/assets/pdf/cv-light.pdf" | relative_url }}" download="[Marco Rosso] Curriculum Vitae.pdf" class="btn" style="text-transform: none;">download curriculum vitae</a>
    </div>

    <!-- Embed the PDF -->
    <div class="pdf-container">
        <embed src="{{ site.baseurl }}/assets/pdf/cv-light.pdf" type="application/pdf">
    </div>
</body>
</html>
